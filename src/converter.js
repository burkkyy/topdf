/**
 * @brief converts docx to pdf
 *
 * @note Only works on linux system
 * @note Assumes libreoffice is installed on system
 * @param filepath path to file to file to convert
 */
export async function docxToPdf(filepath){
}

